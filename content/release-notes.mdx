## Release Notes

Vicenda has not had its first public release yet, so there is nothing for this
page to fetch. When `v0.1.0` ships, every version's changes appear here,
pulled from GitHub Releases — the same feed the app's own updater reads.

export const metadata = {
    description:
        "What's new in Vicenda, pulled from GitHub Releases — the same feed the app's updater reads.",
};
