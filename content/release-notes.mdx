## Release Notes

Vicenda is in a closed beta, and builds go out by hand rather than through a
public release feed — so there is nothing for this page to fetch yet.

When the beta opens, this is where each version's changes will live.

export const metadata = {
    description:
        "What's new in Vicenda. The closed beta ships builds by hand, so there is no public release feed yet.",
};
