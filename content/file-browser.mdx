---
description: "Browse files in a Finder-style outline with live Git status badges, sortable columns, fork awareness, GitHub Issue/PR/Star columns, in-window Quick Look, and a fast file filter."
---

# File Browser

The file browser presents a root folder's contents in an expandable outline table, similar to Finder's list view, but enriched with Git information. Open it by selecting a **root folder** in the sidebar, or by double-clicking a repository in the [Repository List](/docs/repository-list) — FinderGit's flat, all-repos home view.

## Tree navigation

Click the disclosure triangle (▸) next to any directory to expand it and see its contents. The tree loads children lazily — only when you expand a folder.

### Drill into a folder

Double-click a directory (or press <Kbd>↩</Kbd>) to **re-root the tree** on that folder — Finder-style. A back/forward capsule and a clickable breadcrumb appear above the table:

- <Kbd>⌘</Kbd> <Kbd>[</Kbd> — back
- <Kbd>⌘</Kbd> <Kbd>]</Kbd> — forward
- Click any segment in the breadcrumb to jump directly to that depth

![Folder drill-down with back/forward capsule and breadcrumb](/screenshot-drilldown.png)

### Tabs

Open multiple root folders side-by-side with native macOS tabs (<Kbd>⌘</Kbd> <Kbd>T</Kbd> or **File → New Tab**). Each tab has its own navigation, selection, and drill-down history. The window's titlebar and tab labels automatically reflect the current folder you're viewing.

![Two tabs showing different folders — each with its own title](/screenshot-tabs.png)

### Live updates

When files are created, deleted, or renamed externally — from the terminal, a build step, `git clean`, anything — the tree re-enumerates expanded directories within ~300ms. Your selection and scroll position are preserved across the refresh. You shouldn't need <Kbd>⌘</Kbd> <Kbd>R</Kbd> to see what's on disk.

### Hidden files

By default, FinderGit hides dotfiles (files starting with `.`). You can show them in **Settings → General → Show hidden files**.

### Excluded directories

FinderGit automatically skips heavy directories during scanning:
- `node_modules`, `.build`, `Pods`, `Carthage`, `DerivedData`
- `.Trash`, `Library`, `Applications`, `.cache`, `.npm`
- `vendor`, `bower_components`

You can customize this list in **Settings → General → Excluded Folders**.

## Git indicators

### Repository badges

Repositories (directories containing `.git/`) show inline status. The badge reflects both the working-tree state and the local branch's position relative to its upstream:

- **CLEAN** (green) — working tree matches HEAD and the branch is in sync with upstream (or has no upstream)
- **N changes** (orange) — count of uncommitted changes in the working tree (takes priority over any sync state)
- **↑N** (blue) — N commits ahead of upstream (push to sync)
- **↓N** (purple) — N commits behind upstream (pull to sync)
- **↑N ↓N** (red) — diverged: commits on both sides (pull then push, or rebase)

Hover the badge for a tooltip with the exact meaning. Ahead/behind counts are only accurate after a `git fetch` has brought the latest remote refs — Settings → Git → **Fetch on launch** (on by default) runs one fetch at startup so the counts are correct from the first moment.

### Fork badge

GitHub **forks** carry a small branch glyph next to their name. It stays muted when the fork is in sync, and turns orange with **↓N** when the fork is behind its upstream — so you can spot at a glance which forks need syncing. When one does, **Sync Fork** (from the context menu or the Overview tab) updates it on GitHub and pulls your local clone in one step; a fork that has [diverged](/docs/github-integration#update-from-upstream) — its own commits *and* missing upstream ones — can't be fast-forwarded, so the Overview banner offers a merge or a rebase from upstream instead. See [Fork awareness](/docs/github-integration#fork-awareness) for the full story.

### Sub-directory indicators

Directories inside a repository show an orange dot with a number if they contain modified files. This lets you trace dirty files without expanding every folder.

### File status codes

Modified files inside a repository show their `git status` porcelain code (e.g., `M.` for modified-unstaged, `A.` for added-staged, `??` for untracked).

## Root folders

The sidebar is shared with the [Repository List](/docs/repository-list#the-sidebar): its **Library**, **Smart Views** and **Filters** sections drive the flat list, while the **Root Folders** section drives *this* file browser. Root folders are the directories FinderGit scans for Git repositories; the tree opens when you select a root (or a group), or when you double-click a repository in the Repository List.

![The sidebar — Dashboards, Library, Smart Views, Filters, and your Root Folders](/screenshot-sidebar.png)

### Adding and removing roots

- **Drag a folder** from Finder onto the sidebar to add it as a root
- **+** popup menu in the sidebar toolbar — choose **Add Root Folder…** to pick a directory from the macOS Open panel, or **Add Group…** to create a virtual container (see below)
- **−** button removes the currently selected root (its repos drop out of the browser; the actual files on disk are untouched)
- **↻** button rescans every root for new or removed repositories

### Reordering and organising roots

- **Drag** any root to reorder it within the sidebar — position is persisted across launches
- **Right-click → Move to Group** to assign a root to a group, or drag the row directly onto a group header
- **+ → Add Group…** (in the sidebar's `+` popup menu) opens a small dialog to create a virtual group — Tower-style containers that hold roots without changing anything on disk. Useful when you want to keep work, side projects, and dotfiles visually separated
- Groups can be renamed or deleted from their own right-click menu; expanded/collapsed state is sticky like Finder

### Focusing the tree

Click a root to focus the file browser on its contents. Click the row again (or click empty space) to deselect and show every root's contents merged into a single tree.

## Sorting

Click any column header to sort the tree by that column. Supported columns:
- Name, Branch, Status, Issues, PRs, Stars, Size, Date Modified

The count of uncommitted changes now lives inside the **Status** pill (**N changes**) rather than in a separate column.

Click the same header again to reverse the sort order.

## GitHub columns

Three columns pull data from GitHub directly:

- **Issues** — count of open issues
- **PRs** — count of open pull requests
- **Stars** — stargazer count, with a small ⭐ icon

Big numbers use the compact format (`1.2K` instead of `1234`). Sort by Stars to find your most-popular repo at a glance; sort by Issues to surface the busiest.

![File browser with Issues, PRs and Stars columns populated for a folder of Mantine extensions](/screenshot-hero.png)

The columns populate for repos on **GitHub** and **Codeberg**. Codeberg needs no setup at all — it answers anonymously, so the numbers appear whether or not you have connected a GitHub account. Local-only repos, and repos on GitLab or Bitbucket, leave the cells empty rather than showing a placeholder dash on every row.

See [GitHub Integration](/docs/github-integration) for how to enable these columns (either via the `gh` CLI or a Personal Access Token).

## Size column

On repository rows, the **Size** column shows how much disk space the repo's Git data occupies. It's computed lazily — measured per repo only as rows come into view — so it never stalls a large folder. Hover the value for a popover that breaks the number down (Git data on disk). After a cleanup from the [Repo Maintenance](/docs/repo-maintenance) tab, the column refreshes itself to the new, smaller figure. Sort by Size to bring your bulkiest repositories to the top.

## Search and filter

A header strip pinned above the file browser hosts both controls:

- **Filter files** field — filter the tree by name. The filter applies across the entire tree with a 300ms debounce. <Kbd>⌘</Kbd> <Kbd>F</Kbd> focuses the field from anywhere in the window.
- **Git Only** toggle — hide every file and directory that isn't (or isn't inside) a Git repository.

Both controls are scoped to the file browser column and stay anchored above it as the window resizes.

## Selection

Click a row to select it. Standard macOS selection gestures apply:

- <Kbd>⇧</Kbd> click — contiguous range
- <Kbd>⌘</Kbd> click — discontiguous add / remove
- <Kbd>↑</Kbd> / <Kbd>↓</Kbd> with <Kbd>⇧</Kbd> — extend the keyboard selection

The detail panel on the right follows the most recently added item in the selection.

### Bulk actions

When you have multiple files selected, the context menu collapses to a shorter list that operates on the whole set: Open, Reveal in Finder, Copy, Copy Paths, and — when all selected files live in the same repo — **Stage N** / **Unstage N**. At the bottom, **Move N Items to Trash** handles delete.

Press <Kbd>⌫</Kbd> (Delete) to trash the whole selection in one go. <Kbd>⌘</Kbd> <Kbd>Z</Kbd> restores every file to its original path — if a path has been reclaimed in the meantime, the trashed copy is revealed in Finder so nothing silently disappears.

![Multi-selection with the bulk-action context menu](/screenshot-multiselect.png)

## Drag files out

Grab the **icon** of any file or folder and drop it anywhere that accepts a file URL — Finder windows, chat apps, editors, mail composers, the Dock. The text portion of the row stays click-through for selection, so dragging is an explicit gesture.

## Context menu

Right-click any item in the tree to get a context-sensitive menu with inline SF Symbol icons. On a single item the menu adapts to what you right-clicked:

- **Repository folder** — Open in Finder / Terminal, Add to Root Folders, Quick Look, Get Info, Duplicate, Copy / Copy Path, Fetch / Pull / Push, **Open on GitHub** (or GitLab / Bitbucket / Codeberg — the label follows the remote's host; shown only when the repo has a recognized remote), Stage All, Move to Trash
- **File inside a repo** — Open, Quick Look, Reveal in Finder, Get Info, Duplicate, Copy / Copy Path, Stage / Unstage (when applicable), Move to Trash

![Context menu on a repository folder](/screenshot-context-menu.png)

**Finder-parity actions**:

- **Get Info** — opens the system Get Info panel (same window you get from Finder's <Kbd>⌘</Kbd> <Kbd>I</Kbd>)
- **Duplicate** — creates a sibling `… copy` / `… copy 2` next to the original, matching Finder's naming
- **Copy** — copies the actual file reference to the clipboard, paste-able into another Finder window / Mail / Messages / anywhere

## Quick Look

Press **Space** (or **⌘Y**) on any selected file to preview it right inside the window — and Space or Escape closes it again, Finder-style.

- **Source code** opens with full syntax highlighting across dozens of languages, themed to match light and dark mode
- **`.md` and `.markdown`** files render formatted, on a clean uniform background
- **Images, PDFs, and media** open in a native in-window preview, and degrade gracefully when a file type can't be rendered

![Source code preview with syntax highlighting, shown on Space](/screenshot-feature-code-preview.png)
